import strawberry
from src.resolvers.base_resolver import BaseResolver


# class CreateServerResolver(BaseResolver):
#     desc = "creating a new server, [dev] scope"
#
#     @staticmethod
#     def fn(server: = strawberry.UNSET) ->
